// Replaced default Flutter counter smoke test.
// Real widget tests live in test/widget/.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder – real tests are in test/unit/ and test/widget/', () {
    expect(true, isTrue);
  });
}
