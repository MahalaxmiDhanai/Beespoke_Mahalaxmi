name: beespoke_app
description: "Beespoke – Product Feed App with Clean Architecture, Riverpod, and WebView."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State management & DI
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Networking
  dio: ^5.7.0

  # Immutable models & JSON
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0

  # Local storage
  hive_flutter: ^1.1.0

  # In-app browser
  webview_flutter: ^4.10.0

  # Functional error handling
  dartz: ^0.10.1

  # Cached network images
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  integration_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  # Code generation
  build_runner: ^2.4.13
  freezed: ^2.5.7
  riverpod_generator: ^2.6.4
  json_serializable: ^6.8.0
  # hive_generator not needed: we use Box<int>/Box<Map<String,dynamic>>

  # Mocking
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
